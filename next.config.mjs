/** @type {import('next').NextConfig} */
const nextConfig = {
  // Permitir que el servidor ejecute scripts Python desde la carpeta scripts/
  serverExternalPackages: [],
};

export default nextConfig;
